<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:gravity="center_vertical"
    android:id="@+id/place_item_view"
    android:layout_margin="@dimen/default_large_margin"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/place_address"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintVertical_bias="0.0"
        android:text="scarlette is joining"
        android:textStyle="bold"
        android:padding="@dimen/default_large_margin"
        android:textSize="@dimen/default_text_size"
        android:contentDescription="@string/scarlette_is_joining"/>
    <TextView
        android:id="@+id/place_area"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:paddingStart="@dimen/default_large_margin_chat"
        android:textSize="@dimen/default_text_size"
        android:contentDescription="@string/scarlette_is_joining"/>


</LinearLayout>