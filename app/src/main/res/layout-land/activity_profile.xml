<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        layout="@layout/app_bar_profile"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        android:background="@color/colorBgDrawerView"
        app:headerLayout="@layout/nav_header_profile"
        app:menu="@menu/activity_profile_drawer">

<!--    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="@dimen/nav_logo"
        android:contentDescription="@string/nav_header_desc"
        android:layout_gravity="bottom"
        android:layout_marginStart="@dimen/size_image_list_restaurant"
        app:srcCompat="@drawable/ic_logo_auth" />-->
    </com.google.android.material.navigation.NavigationView>

</androidx.drawerlayout.widget.DrawerLayout>
